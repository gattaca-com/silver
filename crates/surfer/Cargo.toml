[package]
name = "silver_surfer"
edition.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
crossterm.workspace = true
flux.workspace = true
hdrhistogram.workspace = true
libc.workspace = true
ratatui.workspace = true
silver_common.workspace = true
silver_metrics.workspace = true
silver_network.workspace = true
silver_peer.workspace = true
silver_storage.workspace = true

[features]
# Fold the producer's perf-events rings into the overseer flamegraph's per-call
# counter columns. Surfer only reads the samples the producer already wrote — no
# rdpmc here — so it needs no perf_event_paranoid privileges itself.
perf = ["silver_metrics/perf"]

[lints]
workspace = true
