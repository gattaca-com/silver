[package]
name = "silver_beacon_state"
edition.workspace = true
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[features]
ef_tests = ["silver_common/test-util"]

[dependencies]
silver_common.workspace = true
silver_beacon_state_data.workspace = true
silver_config.workspace = true
silver_metrics.workspace = true
silver_ssz.workspace = true
flux.workspace = true
ring.workspace = true
blst.workspace = true
rustc-hash.workspace = true
thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
# Exposes SlotTicker::set_current_slot for fork-choice tick tests.
silver_common = { workspace = true, features = ["test-util"] }
# EF fork_choice peerdas DA cases: run the real column-sidecar verification
# (lives in storage; storage only depends on beacon_state_data, so no cycle).
silver_storage.workspace = true
criterion.workspace = true
rand.workspace = true
serde = { version = "1", features = ["derive"] }
serde_yml = "0.0.12"
snap = "0.1"

[[bench]]
harness = false
name = "bls_verify"
